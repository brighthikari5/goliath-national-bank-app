<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="valeria.moscoso.goliathnationalbankapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".presentation.GoliathBankApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic__49_gorilla"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic__49_gorilla"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoliathNationalBankApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.itemdetail.ItemDetailActivity" />
        <activity android:name=".presentation.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>